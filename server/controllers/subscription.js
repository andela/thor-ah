import { User } from '../models';


/**
 * User susbscription Class
*/
class Subscription {
  /**
   * @param {object} req
   * @param {object} res
   * @param {object} next
   * @description dojojodi
   * @returns {object} jsonRes
   */
  static subscribe(req, res, next) {
    // enter subscription function
  }

  /**
   * @param {object} req
   * @param {object} res
   * @param {object} next
   * @description ;pjp
   * @returns {object} jsonResponse
   */
  static unsoscribe(req, res, next) {
    // function goes here
  }
}

export default Subscription;
